# Credenciales Google - copiar manualmente via AnyDesk a C:\pizza_pipeline\
google_key.json

# Slug de locacion - especifico de cada PC
location_slug.txt

# Log del pipeline - especifico de cada PC
pipeline.log
verify_pipeline.log
report.csv

# Carpetas generadas en runtime (se suben vacias con .gitkeep)
frames/*
!frames/.gitkeep
cropped_frames/*
!cropped_frames/.gitkeep
selected_frames/*
!selected_frames/.gitkeep
processed_videos.txt
pending_videos.txt

# Videos
*.mp4

# Python
__pycache__/
*.pyc
*.pyo
.env

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Pipeline simple - no se incluye en el repo principal
pipeline_simple/

# Interruptor por PCBOX: si existe, el pipeline usa seleccion YOLO en vez de ResNet
use_crop_only.enabled

# Reportes locales de la seleccion YOLO
historial_selecciones.csv
reporte_seleccion.html

# Pruebas locales de solo-crop
test_crop_only/
